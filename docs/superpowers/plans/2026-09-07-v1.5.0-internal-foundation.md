# v1.5.0 内部地基与工程欠账 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 还掉六项内部欠账（execCommand 返回值、日志层、prompt 内核、模板外置、类型拆分、PR 门禁），CLI 对用户可见的行为一个字都不变。

**Architecture:** 每一项都是「先加接缝，再把调用点搬过去」。新模块 `src/utils/logger.ts`、`src/templates/commitlint.ts`、`src/types.ts` 承接从既有文件里搬出来的职责；`utils/index.ts` 一律不做 re-export，让 TypeScript 编译失败充当「漏改调用点」的检查器。

**Tech Stack:** TypeScript + tsdown 构建，vitest（单测 mock 一切 / E2E 真跑子进程），execa、yocto-spinner、@clack/prompts、picocolors，GitHub Actions。

**Spec:** `docs/superpowers/specs/2026-09-07-v1.5.0-internal-foundation-design.md`

## Global Constraints

- **`expect(...)` 断言一处不改。** 只允许改 `import` 路径、`vi.mock()` 的目标模块、以及 `vi.mock('execa')` 工厂的返回值形状。需要改断言才能变绿 → 停下来说明，不要顺手改。
- **输出流向不变**：`printWarn`/`printInfo`/`printErr` 走 stdout，`yocto-spinner` 走 stderr。
- **不加没有调用方的 API**（prompt 层不接 confirm/text/multiselect）。
- **代码注释用中文**，commit message 用英文（Conventional Commits），测试名用中文。
- **commit 不加 `Co-Authored-By` trailer**，不推送。
- 每个任务结束前跑 `pnpm typecheck && pnpm lint && pnpm test`；Task 7 之后再跑一次 `pnpm test:e2e`。
- 分支：`dev`。

---

### Task 1: HB-17 `execCommand` 返回执行结果

**Files:**
- Modify: `src/utils/index.ts`（`execCommand`，当前在文件末尾附近）
- Test: `tests/utils.test.ts`（mock 工厂在第 23-26 行，execCommand 用例在第 331 行附近）

**Interfaces:**
- Produces: `ExecOptions { cwd?: string, env?: Record<string, string> }`、`ExecResult { stdout: string, stderr: string, exitCode: number }`、`execCommand(command: string, options?: ExecOptions): Promise<ExecResult>`

- [ ] **Step 1: 先把 execa 的 mock 工厂改成返回真实形状**

`tests/utils.test.ts` 与 `tests/package-manager.test.ts` 两处都改（现在返回 `undefined`，`execCommand` 一读返回值就会崩）：

```ts
vi.mock('execa', async importOriginal => ({
  ...await importOriginal<typeof import('execa')>(),
  execa: vi.fn(async () => ({ stdout: '', stderr: '', exitCode: 0 })),
}))
```

- [ ] **Step 2: 写三条失败测试**

追加到 `tests/utils.test.ts` 里 execCommand 那个 describe 内：

```ts
  it('execCommand 应该把子进程的输出带回来', async () => {
    vi.mocked(execa).mockResolvedValue({ stdout: 'v22.1.0', stderr: '', exitCode: 0 } as never)
    await expect(execCommand('node --version')).resolves.toEqual({
      stdout: 'v22.1.0',
      stderr: '',
      exitCode: 0,
    })
  })

  it('execCommand 不带选项时不应该给 execa 传第三个参数', async () => {
    await execCommand('git init')
    // 多传一个 undefined 会让所有「调用形态」断言失效，这条专门钉住它
    expect(execa).toHaveBeenCalledWith('git', ['init'])
  })

  it('execCommand 带 cwd/env 时应该透传给 execa', async () => {
    await execCommand('git status', { cwd: '/tmp/demo', env: { FOO: 'bar' } })
    expect(execa).toHaveBeenCalledWith('git', ['status'], { cwd: '/tmp/demo', env: { FOO: 'bar' } })
  })
```

- [ ] **Step 3: 跑测试确认失败**

Run: `pnpm vitest run tests/utils.test.ts`
Expected: FAIL —— 第一条因为 `execCommand` 返回 `undefined`，第三条因为它只用两个参数调用 execa。

- [ ] **Step 4: 实现**

替换 `src/utils/index.ts` 里的 `execCommand`：

```ts
export interface ExecOptions {
  /** 子进程的工作目录，不传就继承当前进程 */
  cwd?: string
  /** 传给子进程的环境变量 */
  env?: Record<string, string>
}

export interface ExecResult {
  stdout: string
  stderr: string
  exitCode: number
}

/**
 * 跑一条命令，并把执行结果带回来
 *
 * 不传 options 时必须原样调用 `execa(file, args)`：多传一个 undefined 会让测试里
 * 所有「execa 是用什么参数调的」断言全部失效，而那些断言正是默认路径没被改坏的保护。
 * allowFailure 刻意留在 PackageManager.exec 那一层 —— 挪下来就得给 execa 传
 * `reject: false`，又会多出第三个参数。
 */
export async function execCommand(command: string, options?: ExecOptions): Promise<ExecResult> {
  const [file, ...commandArguments] = parseCommandString(command)
  try {
    const result = options
      ? await execa(file, commandArguments, { cwd: options.cwd, env: options.env })
      : await execa(file, commandArguments)
    return {
      stdout: result.stdout as string,
      stderr: result.stderr as string,
      exitCode: result.exitCode ?? 0,
    }
  }
  catch (e) {
    throw new ScriptError(MSG_FOR.execFailed(command), { cause: e })
  }
}
```

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm vitest run tests/utils.test.ts tests/package-manager.test.ts`
Expected: PASS，且 `package-manager.test.ts` 里 8 处 `toHaveBeenCalledWith('npm', [...])` 全绿（证明默认调用形态没变）。

- [ ] **Step 6: 用变异证明新断言真的在把关**

把实现临时改成无条件传第三个参数：

```ts
const result = await execa(file, commandArguments, { cwd: options?.cwd, env: options?.env })
```

Run: `pnpm vitest run tests/utils.test.ts tests/package-manager.test.ts`
Expected: FAIL —— 「不带选项时不应该给 execa 传第三个参数」这条红，`package-manager.test.ts` 里的调用形态断言也红。确认后**把实现改回 Step 4 的版本**。

- [ ] **Step 7: 全量验证并提交**

```bash
pnpm typecheck && pnpm lint && pnpm test
git add src/utils/index.ts tests/utils.test.ts tests/package-manager.test.ts
git commit -m "refactor: return the execution result from execCommand

doctor will need to read what a command printed, and every check it makes
runs one. Give execCommand a result and optional cwd/env, while keeping the
no-options path calling execa with exactly two arguments so the existing
call-shape assertions stay meaningful."
```

---

### Task 2: HB-19a 建立 logger 模块，收编 print* 与裸 console.log

**Files:**
- Create: `src/utils/logger.ts`
- Create: `tests/logger.test.ts`
- Modify: `src/utils/index.ts`（移出 `printWarn`/`printInfo`/`printErr`，`banner()` 改用 `printLine`）
- Modify: `src/scripts/main.ts`（4 处 `console.log` + 转出给 bin 的 re-export）
- Modify: `src/scripts/commitlint-init.ts`（import）
- Modify: `tests/utils.test.ts`（移走 print* 用例与 import）
- Modify: `tests/commitlint-init.test.ts`（拆 mock）

**Interfaces:**
- Consumes: 无（logger 不 import 本仓库任何模块，避免循环）
- Produces: `printLine(msg?: string): void`、`printWarn(msg: string): void`、`printInfo(msg: string): void`、`printErr(msg: string): void`

- [ ] **Step 1: 先写测试，让它红**

（实现放在 Step 3 —— 先看着它失败，才知道这些用例真的在验证 logger 而不是别的什么。）

把 print* 的现有用例搬进 `tests/logger.test.ts`：从 `tests/utils.test.ts` 剪下 `describe('printInfo')`、`describe('printWarn')`、`describe('printErr')` 三段（**断言一个字不改**），放进新文件，并补一条 `printLine` 的用例：

```ts
import { describe, expect, it, vi } from 'vitest'
import { printErr, printInfo, printLine, printWarn } from '@/utils/logger'

describe('printLine', () => {
  it('应该原样打印一行', () => {
    const spy = vi.spyOn(console, 'log').mockImplementation(() => {})
    printLine('hello')
    expect(spy).toHaveBeenCalledWith('hello')
    spy.mockRestore()
  })
})

// ↓ 这里粘贴从 tests/utils.test.ts 搬过来的三个 describe，原样不动
```

同时把 `tests/utils.test.ts` 顶部 import 里的 `printErr`/`printInfo`/`printWarn` 删掉。

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm vitest run tests/logger.test.ts`
Expected: FAIL —— 找不到模块 `@/utils/logger`

- [ ] **Step 3: 建 logger 模块**

新建 `src/utils/logger.ts`。三个 print* 从 `src/utils/index.ts` **原样搬过来**，空行、颜色、badge 一个字不动：

```ts
import colors from 'picocolors'

const { bgYellow, bgRed, bgCyan } = colors

/**
 * 直接往 stdout 打一行
 *
 * 终端输出的唯一出口。散落各处的裸 console.log 是「哪条消息走哪个流」这类问题的温床 ——
 * 收到这里之后，要改流向或加前缀，只有这一个地方要动。
 */
export function printLine(msg = '') {
  console.log(msg)
}

export function printWarn(msg: string) {
  printLine(' ')
  printLine(`${bgYellow(' WARN ')} ${msg}`)
  printLine(' ')
}

/**
 * 中性提示，用于「结果正确」而非「需要担心」的情况
 *
 * 跳过项目已有的配置是这个工具在正常工作，不是警告 —— 用警告黄渲染会让人读成
 * 「哪里出错了」，久而久之就学会了无视真正要紧的那些消息。
 */
export function printInfo(msg: string) {
  printLine(' ')
  printLine(`${bgCyan(' INFO ')} ${msg}`)
  printLine(' ')
}

export function printErr(msg: string) {
  printLine(' ')
  printLine(`${bgRed(' ERROR ')} ${msg}`)
  printLine(' ')
}
```

- [ ] **Step 4: 跑测试确认通过，再把调用点搬过去**

Run: `pnpm vitest run tests/logger.test.ts` → PASS，然后：

1. `src/utils/index.ts`：删掉三个 print* 函数与 `bgYellow, bgRed, bgCyan` 的解构；顶部加 `import { printLine } from '@/utils/logger'`；`banner()` 里 4 处 `console.log(...)` 全部换成 `printLine(...)`（`console.log('')` → `printLine()`）。
2. `src/scripts/main.ts`：`import { printLine } from '@/utils/logger'`，4 处 `console.log(...)` 换成 `printLine(...)`；把
   `export { printErr, ScriptError } from '@/utils'`
   改成两行：
   ```ts
   export { ScriptError } from '@/utils'
   export { printErr } from '@/utils/logger'
   ```
   （`bin/index.js` 仍然从 `dist/main.js` 拿 `printErr`，路径不变。）
3. `src/scripts/commitlint-init.ts`：从 `@/utils` 的 import 里去掉 `printInfo, printWarn`，新增
   `import { printInfo, printWarn } from '@/utils/logger'`。

- [ ] **Step 5: 拆 `commitlint-init.test.ts` 的 mock**

那份 `vi.mock('@/utils')` 是完整替换式的，把 print* 挪出去：

```ts
vi.mock('@/utils', () => ({
  execCommand: execCommandMock,
  getPackageJSON: getPackageJSONMock,
  isInteractive: isInteractiveMock,
  isTsProject: isTsProjectMock,
  writePackageJSON: writePackageJSONMock,
}))

vi.mock('@/utils/logger', () => ({
  printInfo: printInfoMock,
  printWarn: printWarnMock,
}))
```

- [ ] **Step 6: 全量验证**

Run: `pnpm typecheck && pnpm lint && pnpm test`
Expected: 全绿。TypeScript 会揪出任何漏改的 print* 调用点（因为 `utils/index.ts` 不做 re-export）。

- [ ] **Step 7: 提交**

```bash
git add src/utils/logger.ts src/utils/index.ts src/scripts/main.ts src/scripts/commitlint-init.ts tests/logger.test.ts tests/utils.test.ts tests/commitlint-init.test.ts
git commit -m "refactor: give terminal output a single module

printWarn/printInfo/printErr lived in utils next to filesystem probing, and
bare console.log calls sat in main and banner. Move them into utils/logger
with printLine as the only stdout exit, so a future change to how a line is
rendered has one place to happen. Streams are unchanged."
```

---

### Task 3: HB-19b spinner 封装与接入

**Files:**
- Modify: `src/utils/logger.ts`（新增 `createSpinner`）
- Modify: `src/utils/package-manager.ts`（`uninstall` 改用 `run`）
- Modify: `src/scripts/main.ts`（`spinner().success(...)`）
- Modify: `src/scripts/commitlint-init.ts`（`yoctoSpinner()` → `createSpinner()`，`SetupContext.spinner` 类型）
- Modify: `tests/logger.test.ts`、`tests/commitlint-init.test.ts`（logger mock 补 `createSpinner`）

**Interfaces:**
- Consumes: Task 2 的 `src/utils/logger.ts`
- Produces: `TaskSpinner { start(text?): void, success(text?): void, stop(text?): void, run<T>(texts: { start: string, success: string }, fn: () => Promise<T>): Promise<T> }`、`createSpinner(): TaskSpinner`

- [ ] **Step 1: 写失败测试**

追加到 `tests/logger.test.ts`：

```ts
// vi.mock 会被提升到文件顶部，所以 stub 必须用 vi.hoisted 一起提上去，
// 否则工厂执行时 spinnerStub 还处在暂时性死区
const spinnerStub = vi.hoisted(() => ({
  start: vi.fn(function (this: any) { return this }),
  success: vi.fn(),
  stop: vi.fn(),
}))

vi.mock('yocto-spinner', () => ({
  default: () => spinnerStub,
}))

describe('createSpinner', () => {
  beforeEach(() => {
    spinnerStub.start.mockClear()
    spinnerStub.success.mockClear()
    spinnerStub.stop.mockClear()
  })

  it('run 成功时应该按 start → success 的顺序收尾', async () => {
    const { createSpinner } = await import('@/utils/logger')
    const result = await createSpinner().run({ start: '装依赖', success: '装好了' }, async () => 42)
    expect(result).toBe(42)
    expect(spinnerStub.start).toHaveBeenCalledWith('装依赖')
    expect(spinnerStub.success).toHaveBeenCalledWith('装好了')
    expect(spinnerStub.stop).not.toHaveBeenCalled()
  })

  it('run 失败时应该先停掉转圈再把错误抛出去', async () => {
    const { createSpinner } = await import('@/utils/logger')
    const boom = new Error('boom')
    // 转圈的那行不停下来，会和紧随其后的报错抢同一行
    await expect(createSpinner().run({ start: '装依赖', success: '装好了' }, async () => {
      throw boom
    })).rejects.toBe(boom)
    expect(spinnerStub.stop).toHaveBeenCalled()
    expect(spinnerStub.success).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm vitest run tests/logger.test.ts`
Expected: FAIL with "createSpinner is not a function"

- [ ] **Step 3: 实现**

在 `src/utils/logger.ts` 追加：

```ts
import yoctoSpinner from 'yocto-spinner'

export interface TaskSpinner {
  start: (text?: string) => void
  success: (text?: string) => void
  stop: (text?: string) => void
  /**
   * 跑一段带转圈的任务
   *
   * 抛错时保证先把转圈停掉再把错误原样抛出去 —— 这条纪律原来靠每个调用点自己
   * 手写 try/catch 记着，漏一次，报错信息就会和转圈那行抢地方。
   */
  run: <T>(texts: { start: string, success: string }, fn: () => Promise<T>) => Promise<T>
}

/** yocto-spinner 只在这里出现一次，跟 prompt.ts 只在一处 import @clack/prompts 是同一个道理 */
export function createSpinner(): TaskSpinner {
  const s = yoctoSpinner()

  return {
    start: text => void s.start(text),
    success: text => void s.success(text),
    stop: text => void s.stop(text),
    async run({ start, success }, fn) {
      s.start(start)
      try {
        const result = await fn()
        s.success(success)
        return result
      }
      catch (e) {
        s.stop()
        throw e
      }
    },
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm vitest run tests/logger.test.ts`
Expected: PASS

- [ ] **Step 5: 接入三个调用点**

1. `src/utils/package-manager.ts`：删掉 `import yoctoSpinner from 'yocto-spinner'`，改
   `import { createSpinner } from '@/utils/logger'`，`uninstall` 改成：

```ts
    async uninstall(pkg) {
      try {
        await createSpinner().run(
          { start: MSG.spinnerUninstallStart, success: MSG_FOR.uninstallDone(pkg) },
          () => execCommand(`${name} ${spec.remove}${rootFlag} ${pkg}`),
        )
      }
      catch (e) {
        throw new ScriptError(MSG_FOR.uninstallFailed(pkg), { cause: e })
      }
    },
```

2. `src/scripts/main.ts`：删掉 `import spinner from 'yocto-spinner'`，`spinner().success(MSG.clearDone)` → `createSpinner().success(MSG.clearDone)`（`createSpinner` 从 `@/utils/logger` 一起 import）。
3. `src/scripts/commitlint-init.ts`：删掉 `import yoctoSpinner from 'yocto-spinner'`，`const spinner = yoctoSpinner()` → `const spinner = createSpinner()`；`SetupContext` 里 `spinner: ReturnType<typeof yoctoSpinner>` → `spinner: TaskSpinner`（`import type { TaskSpinner } from '@/utils/logger'`）。**`runSetup` 里那些 start/success 调用保持原样**——它中间夹着「跳过就 stop 后改打 printInfo」的分支，套 `run()` 反而要把分支拆散，这一版不动。

- [ ] **Step 6: 给 commitlint-init 的 logger mock 补上 createSpinner**

`tests/commitlint-init.test.ts` 里那份 `vi.mock('@/utils/logger')` 是完整替换，缺一个导出就会报 undefined：

```ts
vi.mock('@/utils/logger', () => ({
  printInfo: printInfoMock,
  printWarn: printWarnMock,
  createSpinner: () => ({ start: vi.fn(), success: vi.fn(), stop: vi.fn(), run: async (_t: unknown, fn: () => Promise<unknown>) => fn() }),
}))
```

`tests/main.test.ts` 与 `tests/package-manager.test.ts` **不用改**：它们 mock 的是 `yocto-spinner` 本身，而 logger 内部仍然 import 它，mock 照样生效。

- [ ] **Step 7: 全量验证并提交**

```bash
pnpm typecheck && pnpm lint && pnpm test
git add src/utils/logger.ts src/utils/package-manager.ts src/scripts/main.ts src/scripts/commitlint-init.ts tests/logger.test.ts tests/commitlint-init.test.ts
git commit -m "refactor: wrap the spinner so failures always stop it

Stopping the spinner before throwing was a rule each call site had to
remember; uninstall was the only one that did. run() now owns that pairing,
and yocto-spinner is imported in exactly one file."
```

---

### Task 4: HB-18 prompt 内核与 canPrompt

**Files:**
- Modify: `src/utils/prompt.ts`
- Modify: `src/scripts/commitlint-init.ts`（`resolveLinterChoice` 的守卫与 import）
- Modify: `tests/prompt.test.ts`（补新用例）
- Modify: `tests/commitlint-init.test.ts`（mock 迁移）

**Interfaces:**
- Consumes: `isInteractive()` from `@/utils`
- Produces: `canPrompt(): boolean`、`promptSelect<T>(config: { message: string, options: { value: T, label: string }[] }): Promise<T | undefined>`、`promptLinterChoice()`（签名不变）

- [ ] **Step 1: 写失败测试**

追加到 `tests/prompt.test.ts`（文件顶部补一个 `@/utils` 的 mock，避免把 figlet 那堆依赖拉进来）：

```ts
const isInteractiveMock = vi.hoisted(() => vi.fn(() => true))
vi.mock('@/utils', () => ({ isInteractive: isInteractiveMock }))

describe('canPrompt', () => {
  it('可交互时应该返回 true', async () => {
    const { canPrompt } = await import('@/utils/prompt')
    isInteractiveMock.mockReturnValue(true)
    expect(canPrompt()).toBe(true)
  })

  it('不可交互时应该返回 false', async () => {
    const { canPrompt } = await import('@/utils/prompt')
    isInteractiveMock.mockReturnValue(false)
    expect(canPrompt()).toBe(false)
  })
})

describe('promptSelect', () => {
  it('用户取消时应该返回 undefined，并调用 cancel 提示', async () => {
    const { promptSelect } = await import('@/utils/prompt')
    const cancelSymbol = Symbol('cancel')
    selectMock.mockResolvedValue(cancelSymbol)
    isCancelMock.mockImplementation(value => value === cancelSymbol)
    expect(await promptSelect({ message: '选一个', options: [{ value: 'a', label: 'A' }] })).toBeUndefined()
    expect(cancelMock).toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm vitest run tests/prompt.test.ts`
Expected: FAIL with "canPrompt is not a function"

- [ ] **Step 3: 实现**

改写 `src/utils/prompt.ts`：

```ts
import type { LinterKind } from '@/utils/linter'
import { cancel, isCancel, select } from '@clack/prompts'
import { MSG } from '@/constants/messages'
import { isInteractive } from '@/utils'

interface SelectConfig<T> {
  message: string
  options: { value: T, label: string }[]
}

/**
 * 现在这个环境能不能弹交互提示
 *
 * 守卫放在 prompt 层而不是各个脚本里：调用方只需要问「能不能问用户」，
 * 不必知道判据是 TTY 还是 CI 环境变量。
 */
export function canPrompt(): boolean {
  return isInteractive()
}

/**
 * 单选提示的统一入口
 *
 * 「取消 → 打提示 → 返回 undefined」这个约定收在这里，新增一种提示时不必重写一遍。
 */
export async function promptSelect<T extends string>(config: SelectConfig<T>): Promise<T | undefined> {
  const answer = await select({ message: config.message, options: config.options })

  if (isCancel(answer)) {
    cancel(MSG.promptCancelledLabel)
    return undefined
  }

  return answer as T
}

export async function promptLinterChoice(): Promise<LinterKind | 'none' | undefined> {
  return promptSelect<LinterKind | 'none'>({
    message: MSG.promptLinterMessage,
    options: [
      // 三个选项是产品名，不翻译
      { value: 'eslint', label: 'ESLint' },
      { value: 'biome', label: 'Biome' },
      { value: 'oxlint', label: 'Oxlint' },
      { value: 'none', label: MSG.promptLinterNone },
    ],
  })
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm vitest run tests/prompt.test.ts`
Expected: PASS，含原有 5 条 `promptLinterChoice` 用例（它们证明薄封装没改变对外行为）。

- [ ] **Step 5: 脚本层改用 canPrompt**

`src/scripts/commitlint-init.ts`：
- 从 `@/utils` 的 import 里去掉 `isInteractive`；
- `import { canPrompt, promptLinterChoice } from '@/utils/prompt'`；
- `resolveLinterChoice` 里 `if (!isInteractive())` → `if (!canPrompt())`。

`tests/commitlint-init.test.ts`：把 `isInteractive: isInteractiveMock` 从 `vi.mock('@/utils')` 里删掉，改到 prompt 的 mock 里（**沿用同一个 mock 函数，所有断言不变**）：

```ts
vi.mock('@/utils/prompt', () => ({
  promptLinterChoice: promptLinterChoiceMock,
  canPrompt: isInteractiveMock,
}))
```

- [ ] **Step 6: 全量验证并提交**

```bash
pnpm typecheck && pnpm lint && pnpm test
git add src/utils/prompt.ts src/scripts/commitlint-init.ts tests/prompt.test.ts tests/commitlint-init.test.ts
git commit -m "refactor: extract a prompt kernel and move the guard into it

The cancel-handling contract was written inline in the only prompt we have,
and the script layer asked isInteractive() directly. promptSelect owns the
contract now and canPrompt answers the question the caller actually has. No
unused confirm/text wrappers: their shape would be a guess until v1.6.0."
```

---

### Task 5: HB-20 模板外置

**Files:**
- Create: `src/templates/commitlint.ts`
- Create: `tests/templates.test.ts`
- Modify: `src/constants/index.ts`（只剩两个常量）
- Modify: `src/scripts/commitlint-init.ts`（import）
- Modify: `tests/constants.test.ts`（只留两个常量的用例）

**Interfaces:**
- Produces: `CONFIG_COMMITLINT: string`、`CONFIG_COMMITLINT_CZGIT: string`（内容与现在逐字节相同）

- [ ] **Step 1: 先让测试指向新家（红）**

新建 `tests/templates.test.ts`，把 `tests/constants.test.ts` 里除 `DEFAULT_PKG_NAME`/`REPO_URL` 之外的**全部内容原样搬过去**（含 `extractTypes` 辅助函数与那三条 data-URL 求值用例），只改 import：

```ts
import { CONFIG_COMMITLINT, CONFIG_COMMITLINT_CZGIT } from '@/templates/commitlint'
```

`tests/constants.test.ts` 只留 `DEFAULT_PKG_NAME` / `REPO_URL` 的用例，import 同步收窄。

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm vitest run tests/templates.test.ts`
Expected: FAIL —— 找不到模块 `@/templates/commitlint`

- [ ] **Step 3: 搬迁**

新建 `src/templates/commitlint.ts`，把 `src/constants/index.ts` 的第 1-102 行（`COMMIT_TYPES`、`renderTypeEnum`、两份模板）**整段剪切**过去，一个字符都不改。文件头补一句说明：

```ts
/**
 * 写进使用者项目的 commitlint 配置模板
 *
 * 从 constants 里搬出来，是因为 constants 本该放「这个 CLI 自己的常量」，
 * 而这两份是产出物 —— 再来一两个子命令，那个文件就没法读了。
 */
```

`src/constants/index.ts` 只剩：

```ts
export const DEFAULT_PKG_NAME = '@huberyyang/todo-scripts'
export const REPO_URL = 'https://github.com/HuberyYang-Space/todo-scripts'
```

`src/scripts/commitlint-init.ts`：`import { CONFIG_COMMITLINT, CONFIG_COMMITLINT_CZGIT } from '@/constants'` → `from '@/templates/commitlint'`。

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm typecheck && pnpm lint && pnpm test`
Expected: 全绿。

- [ ] **Step 5: 确认构建产物没退化**

Run: `pnpm build && node bin/index.js commitlint-init --help`
Expected: 正常打印 commitlint-init 的帮助文本。
（不要去 `dist/` 里 grep 模板的具名导出 —— 模板会被内联进消费它的 entry，grep 不到是正常的，见 CLAUDE.md。）

- [ ] **Step 6: 提交**

```bash
git add src/templates/commitlint.ts src/constants/index.ts src/scripts/commitlint-init.ts tests/templates.test.ts tests/constants.test.ts
git commit -m "refactor: move the commitlint templates out of constants

constants/index.ts was 105 lines, 100 of them template. The next two
subcommands would have made it unreadable. renderLintStagedConfig stays in
linter.ts on purpose: it renders from the linter spec table rather than
being a static template, and moving it would split that module in half."
```

---

### Task 6: HB-21 `ArgvOptions` 按命令拆类型

**Files:**
- Create: `src/types.ts`
- Modify: `src/utils/index.ts`（删除 `ArgvOptions`）
- Modify: `src/registry.ts`、`src/scripts/main.ts`、`src/scripts/commitlint-init.ts`

**Interfaces:**
- Produces: `GlobalOptions`、`CommitlintInitOptions`、`ParsedOptions`

- [ ] **Step 1: 建类型模块**

新建 `src/types.ts`：

```ts
/**
 * CLI 层的参数类型
 *
 * 拆成三个而不是一个扁平 interface：子命令的签名只声明它自己接受的参数，
 * 读签名就知道这个命令认得哪些 flag。main / registry 拿到的是并集 —— mri 在
 * 知道用户要跑哪个子命令之前就把 argv 解析完了，它本来就只能是并集。
 */
export interface GlobalOptions {
  /** 不管跑哪个子命令都接受，且都在 main.ts 里处理 */
  clear?: boolean
  help?: boolean
  version?: boolean
}

/** commitlint-init 自己的参数 */
export interface CommitlintInitOptions extends GlobalOptions {
  czgit?: boolean
  linter?: string
}

/**
 * mri 解析出来的东西：所有子命令 flag 的并集
 *
 * 因为每个字段都是可选的，接受并集的位置可以直接收下只声明了自己那部分的
 * 子命令 init —— 不需要把 Script 泛型化（SCRIPTS 是异构数组，泛型化只能退化成
 * Script<any>[]，等于没拆）。加子命令时这里补一个交叉类型即可。
 */
export type ParsedOptions = GlobalOptions & CommitlintInitOptions
```

- [ ] **Step 2: 让 tsc 红起来**

删掉 `src/utils/index.ts` 里的 `ArgvOptions` interface。

Run: `pnpm typecheck`
Expected: FAIL —— `registry.ts`、`main.ts`、`commitlint-init.ts` 三处报找不到 `ArgvOptions`。这三处报错正是「调用点清单」。

- [ ] **Step 3: 逐个接上**

1. `src/registry.ts`：`import type { ArgvOptions } from '@/utils'` → `import type { ParsedOptions } from '@/types'`；`load` 的签名 → `load: () => Promise<{ init: (options: ParsedOptions) => Promise<void> }>`。
2. `src/scripts/main.ts`：`import type { ArgvOptions } from '@/utils'` → `import type { ParsedOptions } from '@/types'`；`mri<ArgvOptions>(...)` → `mri<ParsedOptions>(...)`。
3. `src/scripts/commitlint-init.ts`：`import type { ArgvOptions, PackageJsonLike } from '@/utils'` 拆成
   `import type { PackageJsonLike } from '@/utils'` + `import type { CommitlintInitOptions } from '@/types'`；
   文件内 5 处 `ArgvOptions`（`planSetup` 参数、`patchPackageJSON` 参数、`resolveLinterChoice` 参数、`init` 参数、`SetupContext.options`）全部换成 `CommitlintInitOptions`。

- [ ] **Step 4: 验证**

Run: `pnpm typecheck && pnpm lint && pnpm test`
Expected: 全绿。`init` 收窄成 `CommitlintInitOptions` 后仍能被 registry 的 `ParsedOptions` 位置接收——所有字段可选，并集是它的结构子类型。

- [ ] **Step 5: 用变异确认类型真的收窄了**

在 `init` 函数体第一行临时插入：

```ts
  void options.nonexistentFlag
```

Run: `pnpm typecheck`
Expected: FAIL，报 `Property 'nonexistentFlag' does not exist on type 'CommitlintInitOptions'`。
若它编译通过，说明参数类型退化成了 `any`，收窄是假的。确认后**删掉这一行**。

- [ ] **Step 6: 提交**

```bash
git add src/types.ts src/utils/index.ts src/registry.ts src/scripts/main.ts src/scripts/commitlint-init.ts
git commit -m "refactor: split ArgvOptions per command

HB-04 turned flag declarations into data but left every command sharing one
flat options interface. A subcommand's signature now names only the flags it
accepts; main and registry keep the union, because mri parses argv before
anyone knows which subcommand is running."
```

---

### Task 7: HB-35 PR 质量门禁 workflow

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: 写 workflow**

```yaml
name: CI

# The release gate only runs on a pushed v* tag, so until now nothing checked a
# pull request: quality gating lived entirely in the local pre-commit hook, and
# a commit made with --no-verify showed no problem anywhere on the PR page.
on:
  pull_request:
    branches:
      - main
      - dev

# Nothing here writes anything.
permissions:
  contents: read

# A second push to the same PR makes the previous run pointless.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # Must come before setup-node, otherwise `cache: pnpm` cannot find pnpm.
      # Which version gets installed is decided by package.json's packageManager.
      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: lts/*
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # The same four commands as the release gate in release.yml, spelled out
      # without `nr` (which needs a global @antfu/ni that CI does not have).
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm test
      # Builds dist/ itself via globalSetup, so no separate build step is needed.
      - run: pnpm test:e2e
```

- [ ] **Step 2: 本地校验 YAML 能被解析**

```bash
node -e "const {parse}=require('yaml');const fs=require('fs');const d=parse(fs.readFileSync('.github/workflows/ci.yml','utf8'));console.log(JSON.stringify(Object.keys(d)),JSON.stringify(d.jobs.quality.steps.map(s=>s.run||s.uses)))"
```

Expected: 打印出 `["name","on","permissions","concurrency","jobs"]` 与 7 个步骤，其中最后四个是四条 `pnpm` 命令。

- [ ] **Step 3: 与 release.yml 逐条比对门禁命令**

```bash
grep -E "^      - run: pnpm (typecheck|lint|test)" .github/workflows/release.yml .github/workflows/ci.yml
```

Expected: 两个文件各出现四条，且逐字相同。

- [ ] **Step 4: 确认 release.yml 一行未改**

```bash
git diff --stat .github/workflows/release.yml
```

Expected: 无输出。

- [ ] **Step 5: 提交**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: gate pull requests on the same four checks as a release

Quality gating lived only in the local pre-commit hook, so a commit made with
--no-verify showed nothing on the PR page. release.yml is untouched: the two
workflows answer different questions, and sharing one would make releasing
depend on the PR path's configuration."
```

---

## 收尾（全部任务完成后）

- [ ] **完整回归**：`pnpm typecheck && pnpm lint && pnpm test && pnpm test:e2e`，粘贴实际输出（单测应为 231 + 新增用例，E2E 57）。
- [ ] **真实运行核对**：在一个临时目录里跑一次 `node bin/index.js commitlint-init`，读一遍它打印的东西——`pnpm test` 只能证明实现与测试一致，证明不了输出对人类是否仍然正确。
- [ ] **更新 ROADMAP.md**：六条状态改 ✅，清空「进行中」，时间轴补一行。
- [ ] **开 PR 到 main**，PR 页面会成为 `ci.yml` 的第一个真实用例。
